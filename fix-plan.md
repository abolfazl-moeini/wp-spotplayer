# پلن رفع Fatal و ترمیم ایمن customer ID سفارش WooCommerce

## بازبینی کد فعلی — ۲۱ اوت ۲۰۲۶ (بدون پیاده‌سازی)

نتیجهٔ بررسی نسخهٔ فعلی `wp-spotplayer.php`:

- باگ از نظر مسیر داده وجود دارد: وقتی `WC_Order::get_customer_id()` صفر است، `get_user()` کاربر معتبری برنمی‌گرداند و کد سفارشی‌ای که به `$user->ID` تکیه دارد، context معتبر ندارد.
- در حال حاضر `spot_woo_license_data_eval()` هنگام تولید دادهٔ لایسنس، یعنی حتی در رندر متاباکس سفارش و سایر مسیرهای read، این کار را انجام می‌دهد:
  `$order->set_customer_id( $maybe_user->ID ); $order->save();`
  بنابراین عملیات ترمیم خودکار هم‌اکنون در لایهٔ نادرست و با زمان اجرای پرریسک وجود دارد؛ این باید در پیاده‌سازی بعدی حذف شود، نه اینکه گسترش پیدا کند.
- fallback فعلی فقط `get_user_by( 'email', ... )` و متای `nikamooz_mobile` را بررسی می‌کند. فهرست کلیدهای مجاز، کنترل یکتایی/نتیجهٔ resolver و مسیر `ambiguous`/`not_found` وجود ندارد.
- نرمال‌ساز فعلی برای شمارهٔ نامعتبر یا خالی رشتهٔ خالی می‌دهد و مقایسهٔ دو رشتهٔ خالی می‌تواند false positive ایجاد کند. هر دو شماره باید واقعاً معتبر و غیرخالی باشند.
- evaluator فعلی خطاهای `eval()` را با `Throwable` می‌گیرد، پس بخشی از خطای `$user` به‌جای fatal به `null` تبدیل می‌شود؛ اما این جایگزین context صریح `WP_User|null`، قرارداد مستند و تست مسیر متاباکس نیست.
- ذخیره با API رسمی WooCommerce انجام شده و از نظر HPOS از دست‌کاری مستقیم `_customer_user` بهتر است، اما چون در read رخ می‌دهد همچنان می‌تواند مالکیت سفارش را بدون اقدام صریح مدیر تغییر دهد.
- hook فعلی `woocommerce_process_shop_order_meta` به‌تنهایی هنوز برای ادعای پشتیبانی دقیق و اجرای یک‌باره در هر دو storage کافی نیست؛ قبل از پیاده‌سازی باید behavior واقعی نسخهٔ نصب‌شدهٔ WooCommerce در legacy و HPOS تست شود.

### پاسخ به ایدهٔ اصلاح دستی

ایدهٔ شما از نظر اثر نهایی درست است، اما باید با API سفارش ووکامرس انجام شود، نه با `update_post_meta()` یا SQL مستقیم. معادل امن عملیات دستی، پس از احراز شرایط، این است:

```php
$order->set_customer_id( $user_id );
$order->save();
```

با این حال، این کد فقط باید در save صریح سفارش توسط مدیر اجرا شود. پیدا کردن کاربر صرفاً با نام، یا با ایمیل/موبایل ناقص، نباید باعث اتصال خودکار شود. تطبیق باید یکتا باشد؛ در غیر این صورت فقط notice راهنمای انتخاب دستی مشتری نمایش داده شود.

### وضعیت تصمیم و محدودهٔ بعدی

- `wp-spotplayer`: فقط evaluator read-only و context ایمن؛ هیچ ترمیمی در رندر متاباکس، preview، checkout یا درخواست ساخت لایسنس انجام نشود.
- لایهٔ هستهٔ مشترک: گزینهٔ ترجیحی برای resolver و ترمیم هنگام save معتبر ادمین، با API رسمی WooCommerce و پشتیبانی legacy/HPOS.
- fallback اجرایی فقط در صورتی مجاز است که هستهٔ مشترک در scope نباشد: همان ترمیم در مسیر save ادمینِ سفارش، با nonce/capability و همان resolver دقیق؛ نه در مسیر مشاهده.
- این مرحله فقط بررسی و به‌روزرسانی plan است؛ هیچ پیاده‌سازی کدی در `wp-spotplayer` انجام نشده است.

## نتیجهٔ بررسی

این سفارش‌ها الزاماً سفارش مهمان واقعی نیستند. ممکن است نام/ایمیل مشتری در اطلاعات صورتحساب و بعضی بخش‌های WooCommerce دیده شود، اما `customer_id` سفارش (و در storage قدیمی، مقدار `_customer_user`) برابر `0` مانده باشد.

این ناسازگاری باگ اسپات‌پلیر را فعال می‌کند:

1. رندر متاباکس `spot_woo_admin_order_box()` به `spot_woo_license_data()` می‌رسد.
2. اگر `_spotplayer_data` خالی باشد، `spot_woo_license_data_eval()` کد سفارشی تنظیم‌شده را اجرا می‌کند.
3. برای customer ID صفر، `WC_Order::get_user()` ممکن است کاربر برنگرداند و fallback فعلی نیز ممکن است ناموفق باشد.
4. `$user` قبل از `compact( 'order', 'user' )` تضمین‌شده مقداردهی نشده است؛ سپس عبارت `eval()` می‌تواند به `$user` تکیه کند و رندر سفارش را دچار خطا کند.

## تصمیم ریسک

اتصال خودکار سفارش به کاربر، فقط یک اصلاح متا نیست: می‌تواند مالکیت سفارش و نمایش آن در «سفارش‌های من» را تغییر دهد. بنابراین این عملیات نباید:

- هنگام مشاهدهٔ سفارش یا رندر متاباکس (`GET`) انجام شود؛
- هنگام checkout برای کاربر لاگ‌اوت صرفاً بر اساس نام، ایمیل یا موبایل انجام شود؛
- با `update_post_meta()` یا SQL مستقیم انجام شود، زیرا افزونه HPOS را پشتیبانی می‌کند و storage ووکامرس ممکن است post meta نباشد.

مسیر کم‌ریسک پیشنهادی: **ترمیم مشترک در لایهٔ هسته فقط هنگام ذخیرهٔ صریح سفارش توسط مدیر**. این منطق باید فقط در یک ماژول مشترک مانند `nikamooz-core / ExtendWC` قرار گیرد، نه در اسپات‌پلیر؛ اسپات‌پلیر فقط context امن و read-only برای تولید لایسنس فراهم می‌کند.

## قرارداد شناسایی کاربر

یک resolver مشترک و تست‌پذیر بسازید که نتیجهٔ آن یکی از این سه حالت باشد: `resolved`، `ambiguous` یا `not_found`.

- ابتدا customer ID فعلی سفارش را با `WC_Order::get_customer_id()` بررسی کند.
- برای سفارش با customer ID صفر، فقط یک کاربر یکتا را با معیار قطعی پیدا کند: ایمیل صورتحساب دقیق **و** شمارهٔ موبایل نرمال‌سازی‌شدهٔ دقیق که هر دو به همان کاربر تعلق دارند.
- نام نمایشی، نام و نام خانوادگی، یا تنها یکی از ایمیل/موبایل برای ترمیم خودکار کافی نیست.
- resolver باید `WP_User|null` را همواره به‌صورت صریح برگرداند؛ هرگز متغیر تعریف‌نشده به evaluator ارسال نشود.
- اگر سایت برای بعضی کاربران دادهٔ موبایل را در کلیدهای متفاوت ذخیره می‌کند، کلیدهای مجاز (`digits_phone`، `billing_phone`، `nikamooz_mobile`) در یک configuration/documented list قرار گیرند و هر مقدار پیش از مقایسه نرمال‌سازی شود.

## تغییرات پیشنهادی در `wp-spotplayer`

1. **ایمن‌سازی evaluation بدون write**
   - `spot_woo_license_data_eval()` باید context صریح زیر را به `spot_eval_license_code()` بدهد: `order => WC_Order` و `user => WP_User|null`.
   - حذف `compact()` روی `$user` احتمالیِ تعریف‌نشده.
   - `set_customer_id()` و `save()` را از این تابع حذف کنید؛ باز کردن صفحهٔ سفارش باید read-only بماند.

2. **سازگاری کد لایسنس با مشتریِ نامشخص**
   - قرارداد کد سفارشی در صفحهٔ تنظیمات ثبت شود: در سفارشِ هنوز ترمیم‌نشده، `$user` می‌تواند `null` باشد.
   - کد پیش‌فرض `spot_license_code()`، به‌ویژه مسیر Digits، باید در نبود کاربر از شمارهٔ صورتحساب fallback امن داشته باشد.
   - `spot_eval_license_code()` خطای عبارت سفارشی را به خروجی بی‌خطر (`null`/آرایهٔ خالی) تبدیل کند و پیام تشخیصی محدود برای مدیر فراهم کند؛ جزئیات حساس یا متن کامل عبارت در UI نشان داده نشود.

3. **مرور مسیرهای مصرف‌کننده**
   - `spot_woo_admin_order_box()`، `spot_woo_order_license_request()` و پیش‌نمایش تنظیمات باید از همین context read-only استفاده کنند.
   - `spot_edd_license_data_eval()` فقط برای حفظ قرارداد مشترک evaluator بازبینی شود؛ اتصال مشتری EDD جزو این اصلاح نیست.

## تغییرات پیشنهادی در هستهٔ مشترک (`nikamooz-core / ExtendWC`)

1. **زمان اجرای ترمیم**
   - یک callback فقط در درخواست ذخیرهٔ سفارشِ ادمین ثبت شود. hook نهایی باید با نسخهٔ نصب‌شدهٔ WooCommerce و هر دو حالت HPOS/legacy سازگار باشد؛ پیش از پیاده‌سازی، با یک تست عملی مشخص شود که callback در «Update order» برای هر دو storage دقیقاً یک‌بار اجرا می‌شود.
   - callback باید nonce استاندارد صفحه، capability ویرایش همان سفارش و وضعیت درخواست مدیریتی را بررسی کند.
   - اگر مدیر از UI استاندارد WooCommerce مشتری را انتخاب کرده باشد، همان انتخاب اولویت قطعی دارد و resolver نباید آن را تغییر دهد.

2. **نحوهٔ ذخیره‌سازی**
   - فقط اگر `get_customer_id() === 0` و resolver نتیجهٔ `resolved` برگرداند، از API رسمی استفاده شود:
     - `$order->set_customer_id( $user_id )`
     - `$order->save()`
   - از دست‌کاری مستقیم `_customer_user` اجتناب شود. WooCommerce خود storage سازگار با HPOS را به‌روز می‌کند.
   - عملیات idempotent باشد: پس از ترمیم، ذخیره‌های بعدی تغییر دوباره یا order note تکراری ایجاد نکنند.

3. **بازخورد و ردپا**
   - در ترمیم موفق، یک order note کوتاه ثبت شود: شناسهٔ مدیر انجام‌دهنده، زمان و شناسهٔ کاربر متصل‌شده؛ از ذخیرهٔ شماره موبایل یا ایمیل کامل در note پرهیز شود.
   - برای حالت `ambiguous` یا `not_found` هیچ تغییری اعمال نشود و یک notice غیرحساس مدیر را به انتخاب دستی مشتری از UI ووکامرس راهنمایی کند.

## ماتریس آزمون و پذیرش

| سناریو | نتیجهٔ مورد انتظار |
| --- | --- |
| customer ID صفر، بدون کاندید قطعی، کد سفارشی دارای `$user->ID` | صفحهٔ ویرایش و متاباکس بدون Fatal باز می‌شوند؛ هیچ write انجام نمی‌شود. |
| customer ID صفر، کد لایسنس فقط متکی به `$order` | پیش‌نمایش/ایجاد لایسنس دادهٔ درست تولید می‌کند. |
| customer ID صفر، یک کاربر با ایمیل و موبایل دقیق منطبق | با باز کردن صفحه تغییری ندارد؛ با «Update order» توسط مدیر، customer ID از طریق API ووکامرس ثبت و یک note ایجاد می‌شود. |
| ایمیل مشابه ولی موبایل متفاوت، یا چند کاربر کاندید | ترمیم خودکار رخ نمی‌دهد؛ مشتری فعلی دست‌نخورده می‌ماند و مدیر مسیر انتخاب دستی را می‌بیند. |
| customer ID معتبر از ابتدا | هیچ override یا note جدیدی ایجاد نمی‌شود؛ کدهای وابسته به `$user` برقرار می‌مانند. |
| کد سفارشی نامعتبر | Fatal رخ نمی‌دهد؛ خروجی بی‌خطر و گزارش تشخیصی کنترل‌شده تولید می‌شود. |
| هر سناریوی بالا در HPOS و storage قدیمی | پس از reload، `get_customer_id()` مقدار صحیح را برمی‌گرداند و callback بیش از یک‌بار عمل نمی‌کند. |

## معیار اتمام

- هیچ مسیر اسپات‌پلیر متغیر `$user` تعریف‌نشده را به `eval` نفرستد.
- باز کردن سفارش هیچ داده‌ای را تغییر ندهد.
- ترمیم customer ID فقط با ذخیرهٔ معتبر ادمین، تطبیق یکتای چندشناسه‌ای و API رسمی `WC_Order` انجام شود.
- مسیر checkout، سفارش‌های نامشخص و فایل غیرمرتبط `.htaccess` خارج از این تغییر باقی بمانند.
