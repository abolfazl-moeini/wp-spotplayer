# برنامه توسعه و بهینه‌سازی تجربه کاربری (TODO: License Creation Modal & Private Notes)

## هدف (Objective)
بهبود فرآیند ایجاد دستی لایسنس اسپات‌پلیر در صفحه ویرایش سفارش ووکامرس (`shop_order`) از طریق:
1. جایگزینی سابمیت کلاسیک صفحه با **ایجکس (AJAX) و پنجره پاپ‌آپ (Modal Dialog)** برای نمایش لحظه‌ای وضعیت.
2. کنترل و پالایش یادداشت‌های سفارش جهت **ثبت فقط موارد مهم و قطعی در یادداشت‌های خصوصی (Private Notes)** و جلوگیری از شلوغ شدن لاگ سفارش.

---

## ۱. مشخصات پاپ‌آپ و رابط کاربری (Modal / Popup UX)

- **رفتار هنگام کلیک بر روی «ایجاد لایسنس»:**
  - جلوگیری از رفرش کامل برگه (`e.preventDefault()`).
  - باز شدن یک مدال تعاملی تمیز (با استفاده از SweetAlert2 یا کامپوننت مودال استاندارد وردپرس).
  - نمایش وضعیت پیشرفت به صورت زنده:
    - حالت در حال پردازش: نمایش لودر و پیام «در حال استعلام و ارسال به سرور اسپات‌پلیر...».
    - حالت موفقیت: نمایش تیک سبز، شناسه لایسنس، کلید اختصاصی، دوره‌های متصل‌شده و دکمه بستن/کپی.
    - حالت خطا: نمایش شفاف متن خطای دریافت شده از سرور اسپات‌پلیر (مانند «یکی از متن‌های واترمارک قبلا برای یکی از دوره‌ها استفاده شده است») به همراه راهنمای رفع آن، بدون رفرش صفحه.

---

## ۲. پالایش یادداشت‌های سفارش (Order Notes Policy)

- **وضعیت فعلی:**
  - به ازای هر بار کلیک یا خطای سرور، بلافاصله یک یادداشت در سفارش ثبت می‌شود که در صورت تلاش‌های مکرر مدیر، یادداشت‌های زیادی ایجاد می‌گردد.
- **بهینه‌سازی پیشنهادی:**
  - خطاهای آزمایشی یا تکراری در حین کار در مدال صرفاً به مدیر در همان پاپ‌آپ نشان داده شود و یادداشت سفارش شلوغ نشود.
  - **فقط رویدادهای کلیدی زیر در یادداشت خصوصی (Private Note) ثبت شوند:**
    ۱. ایجاد قطعی و موفقیت‌آمیز لایسنس جدید به همراه لینک مستقیم پنل اسپات‌پلیر و شناسه یکتا.
    ۲. تغییر یا انتساب لایسنس به سفارش توسط مدیر (Retrieve / Attach).
    ۳. حذف یا ابطال صریح لایسنس.

---

## ۳. معماری فنی پیشنهادی (Technical Architecture)

- **اندپوینت AJAX اختصاصی:**
  - اکشن: `wp_ajax_spot_ajax_create_license`
  - اعتبارسنجی Nonce اختصاصی (`check_ajax_referer`) و سطح دسترسی کاربر (`edit_shop_orders`).
  - ورودی: `order_id`، فیلدهای نام و واترمارک (در صورت ویرایش در مودال).
  - خروجی: پاسخ ساختاریافته JSON شامل:
    ```json
    {
      "success": true,
      "data": {
        "license_id": "6aaac447d02766abe15d5c17",
        "key": "...",
        "url": "..."
      }
    }
    ```
- **اسکریپت و استایل:**
  - انتقال هندلر کلیک به `admin.js` یا اسکریپت سبک در صفحه سفارش.
